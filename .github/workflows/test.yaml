name: tests

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: install
      run: pip install -r requirements.txt

    - name: run tests
      run: pytest
